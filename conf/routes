# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
->      /v1/users                   v1.users.UserRouter
->      /v1/requests                v1.requests.DeliveryRequestRouter